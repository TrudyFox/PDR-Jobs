@using PDR_Jobs
@using System

<div class="alert alert-secondary mt-4" role="alert">

    <span class="oi oi-pencil mr-2" aria-hidden="true"></span>
    @if (TheBodyShop != null)
    {
        <strong>@TheBodyShop.Name</strong>
        <br />

        @if (TheBodyShop.HomePage != null)
        {
            <a href="@TheBodyShop.HomePage"><button> @TheBodyShop.Name Web Page</button></a>
        }
    }
<span class="text-nowrap">
    @if (TheBodyShop.Address != null)
    {
       <AddressComponent Thebodyshop = "BodyShop.address " />//call new ;) adresscomponent

    }

</span>
</div>
@code {

    [Parameter]
    public BodyShop TheBodyShop { get; set; }

}
